logging:
  level:
    root: WARN
  file:
    name: log.txt
    path: /

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_hw4
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    properties.hibernate:
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate